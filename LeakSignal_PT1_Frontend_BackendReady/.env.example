# Local FastAPI backend
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# true  = UI can fall back to PT1 demo data when backend is unavailable
# false = backend errors are exposed (recommended during integration/testing)
NEXT_PUBLIC_USE_MOCKS=false
